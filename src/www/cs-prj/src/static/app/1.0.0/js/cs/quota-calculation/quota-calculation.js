/**
 * Created by lina on 2017/3/7.
 */

+function ($, app) {

    /*点击额度计算按钮 执行的功能*/
    $("#quotaSub").click(function(){
       /* $.getJSON(app.REPAYMENT_WAY + '?a=' + $('#a').val(), function (data) {
            var tpl = Handlebars.compile($('#quota-result-template').html());
            var html = tpl(data);
            $("#quota-result").html(html);
        });*/
    });
}(window.jQuery, window.app);

