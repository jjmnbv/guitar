$(function () {
    +function($, app) {
        /**
         * 初始化列表数据
         */
        $('#tab1,#tab2,#tab3').pagination({
            "first-store": null
        });
    } (window.jQuery, window.app);
});